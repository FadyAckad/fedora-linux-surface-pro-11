# Surface Pro 11 (Snapdragon X Elite): CPU frequency scaling. Fedora builds scmi-cpufreq as a module that does not load
# on its own (modpost cannot generate aliases for the SCMI bus); Fedora's "Snapdragon WoA Laptop Install" wiki page
# prescribes loading it this way. Installed as /usr/lib/modules-load.d/sp11-scmi-cpufreq.conf.
scmi-cpufreq
